server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      #服务注册中心地址
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
        namespace: da8f0071-57d8-4647-8f4c-70374f3d539a
        group: DEV_GROUP

